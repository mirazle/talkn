import Parser from 'ua-parser-js';

export default class User{
  constructor(){

    // talkn datas .
    const id = '';
    const type = '';
    const inputPost = '';
    const inputSearch = '';

    return {
      id,
      type,
      inputPost,
      inputSearch,
    };
  }
}
