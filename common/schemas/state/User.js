import Schema from '../Schema';

export default class User extends Schema{
  constructor(){
    super();
    // talkn datas .
    const id = '';
    const type = '';
    const inputPost = '';
    const inputSearch = '';

    return {
      id,
      type,
      inputPost,
      inputSearch,
    };
  }
}
